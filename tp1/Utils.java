package com.example.applications;

import javafx.beans.binding.IntegerBinding;
import javafx.collections.ObservableArray;
import javafx.collections.ObservableList;
import javafx.scene.control.TableColumn;

import java.util.ArrayList;

public class Utils {
   // public static void insererEnOrdre(ObservableList<FeuilleResultat> liste, FeuilleResultat element) {
    //    ArrayList f = new ObservableArray();
    //}

    public byte moyenneEval(TableColumn liste) {
        short total;
        liste.get
        //for (int i = 0; i < liste.get
    }
}
